# 🚀 BruV Enterprise - 快速啟動指南

## 方式一：一鍵啟動腳本 ⭐ 推薦

### Windows 用戶

雙擊執行以下任一腳本：

```
START.bat         # 批次檔腳本（推薦）
START.ps1         # PowerShell 腳本
```

**啟動選項：**
1. **GUI 啟動器**（最推薦）- 圖形化界面，可視化控制所有服務
2. **快速啟動** - 啟動後端 + 前端，適合開發
3. **僅後端** - 只啟動 API 服務
4. **完整系統** - 啟動所有服務（Docker + 後端 + 前端）

---

## 方式二：GUI 啟動器

直接雙擊：
```
start_gui_launcher.bat
```

**功能：**
- ✅ 一鍵啟動/停止所有服務
- ✅ 實時查看服務日誌
- ✅ 智能狀態檢測
- ✅ 中英文雙語界面
- ✅ 自動打開瀏覽器

---

## 方式三：手動啟動（適合開發者）

### 1. 啟動後端

**PowerShell:**
```powershell
cd BruV_Project
.\start_backend.ps1
```

**或直接用 Python:**
```bash
cd BruV_Project
python -m uvicorn app_anytype:app --host 127.0.0.1 --port 8000 --reload
```

### 2. 啟動前端

```bash
cd BruV_Project/frontend
npm run dev
```

### 3. 啟動 Docker 服務（可選）

```bash
cd BruV_Project
docker-compose up -d
```

---

## 方式四：VSCode 集成啟動

在 VSCode 中按 `F5` 可啟動調試模式。

或使用 VSCode 任務：
- 按 `Ctrl+Shift+P`
- 輸入 "Tasks: Run Task"
- 選擇 "Start BruV Backend"

---

## 服務訪問地址

啟動成功後，可訪問以下地址：

| 服務 | 地址 | 說明 |
|------|------|------|
| 🌐 前端界面 | http://localhost:5173 | Vue 3 應用 |
| 📚 API 文檔 | http://localhost:8000/docs | Swagger 文檔 |
| 🔗 後端 API | http://localhost:8000 | FastAPI 服務 |
| 🤖 Dify | http://localhost:3000 | AI 工作流平台 |
| 📄 RAGFlow | http://localhost:9380 | 文檔檢索系統 |

---

## 常見問題

### Q1: 提示找不到虛擬環境？

**解決：**
```bash
# 創建虛擬環境
python -m venv .venv

# 激活虛擬環境
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac

# 安裝依賴
pip install -r requirements.txt
```

### Q2: Docker 服務無法啟動？

**解決：**
1. 確保已安裝 Docker Desktop
2. 檢查 Docker 是否運行：`docker ps`
3. 手動啟動 Docker Desktop

### Q3: 前端啟動失敗？

**解決：**
```bash
cd frontend
npm install  # 安裝依賴
npm run dev  # 重新啟動
```

### Q4: 端口被占用？

**解決：**
```powershell
# 查看端口占用
netstat -ano | findstr :8000
netstat -ano | findstr :5173

# 結束進程
taskkill /PID <PID> /F
```

---

## 停止服務

### 停止後端/前端
在各自的終端窗口中按 `Ctrl+C`

### 停止 Docker 服務
```bash
docker-compose down
```

### 使用 GUI 啟動器
點擊 "🛑 停止系統" 按鈕

---

## 快速命令參考

```bash
# 完整啟動（推薦新手）
START.bat

# 僅開發環境
.\start_backend.ps1
cd frontend && npm run dev

# 完整系統（生產環境）
docker-compose up -d
python -m uvicorn app_anytype:app --host 0.0.0.0 --port 8000

# 停止所有服務
docker-compose down
# 然後 Ctrl+C 關閉其他終端
```

---

## 系統需求

- **Python**: 3.10+
- **Node.js**: 16+
- **Docker**: 20.10+（可選）
- **操作系統**: Windows 10/11, Linux, macOS

---

## 技術支持

如遇問題，請查看：
- 📖 [完整文檔](./README.md)
- 🐛 [問題追蹤](https://github.com/brucecheng886-png/-/issues)
- 📝 [更新日誌](./CHANGELOG.md)

---

**Enjoy your BruV Enterprise experience! 🎉**
