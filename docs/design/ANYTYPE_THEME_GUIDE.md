# 🎨 BruV Launcher - Anytype Theme Design Document

## 📋 主題變更概覽

### 🔄 從 Cyberpunk 到 Anytype

| 元素 | Cyberpunk (舊) | Anytype (新) |
|------|---------------|-------------|
| **設計理念** | 霓虹發光、未來感 | 極簡扁平、深空黑 |
| **背景** | 藍色漸層 (#0f172a → #1e293b) | 純黑 (#191919) |
| **側邊欄** | 半透明深藍 (rgba) | 實色極夜黑 (#111111) |
| **邊框** | 粗亮藍 (2px #6366f1) | 極細深灰 (1px #333333) |
| **圓角** | 15px | 12px (更銳利) |
| **主色調** | 藍紫色 (#6366f1, #818cf8) | Anytype Blue (#335eea) |
| **警示色** | 紅色漸層 | Alert Red (#e03131) 扁平 |
| **文字** | 亮藍白 (#e2e8f0) | 灰白 (#e5e5e5) |
| **次要文字** | 藍灰 (#8be9fd) | 暗灰 (#888888) |
| **控制台** | 青色 (#22d3ee) | 終端綠 (#4ade80) |

---

## 🎨 Anytype Theme 配色方案

### 主要顏色 (Primary Colors)

```css
/* 背景層級 */
--void-black: #0f0f0f;        /* 最深層（控制台）*/
--night-black: #111111;       /* 側邊欄 */
--deep-gray: #191919;         /* 主背景 */
--card-bg: #262626;           /* 卡片背景 */
--card-hover: #333333;        /* 卡片 Hover */

/* 文字 */
--text-primary: #e5e5e5;      /* 主要文字（灰白）*/
--text-secondary: #aaaaaa;    /* 次要文字 */
--text-muted: #888888;        /* 暗淡文字 */
--text-subtle: #555555;       /* 極淡文字 */

/* 邊框 */
--border-subtle: #2a2a2a;     /* 極細邊框 */
--border-default: #333333;    /* 標準邊框 */
--border-strong: #444444;     /* 強調邊框 */

/* 強調色 */
--anytype-blue: #335eea;      /* 主要動作（啟動）*/
--anytype-blue-hover: #2651dd;
--anytype-blue-pressed: #1e3faf;

--alert-red: #e03131;         /* 警告動作（停止）*/
--alert-red-hover: #c92a2a;
--alert-red-pressed: #a61e1e;

/* 狀態色 */
--terminal-green: #4ade80;    /* 控制台文字 */
--disabled-gray: #555555;     /* 禁用狀態 */
```

---

## 🧩 組件設計規範

### 1️⃣ 主視窗 (#mainWidget)

**Before (Cyberpunk)**:
```css
background: qlineargradient(#0f172a → #1e293b);
border: 2px solid #6366f1;
border-radius: 15px;
```

**After (Anytype)**:
```css
background: #191919;  /* 純色深灰 */
border: 1px solid #333333;  /* 極細邊框 */
border-radius: 12px;  /* 更銳利 */
```

**設計語言**:
- ✅ 移除漸層，使用純色
- ✅ 減小邊框粗細 (2px → 1px)
- ✅ 略微降低圓角 (15px → 12px)

---

### 2️⃣ 標題列 (#titleBar)

**Before**:
```css
background: rgba(15, 23, 42, 0.95);
border-bottom: 1px solid rgba(99, 102, 241, 0.3);
```

**After**:
```css
background: #111111;  /* 實色極夜黑 */
border-bottom: 1px solid #333333;  /* 深灰邊框 */
```

**按鈕變化**:
- 最小化按鈕：透明 → Hover #262626
- 關閉按鈕：Hover 保持紅色 (#e03131)

---

### 3️⃣ 側邊欄 (#sidebar)

**Before**:
```css
background: rgba(15, 23, 42, 0.95);  /* 半透明 */
border-right: 1px solid rgba(99, 102, 241, 0.3);
```

**After**:
```css
background: #111111;  /* 實色黑 */
border-right: 1px solid #333333;  /* 深灰 */
```

**視覺效果**:
- ✅ 移除半透明效果，增加扎實感
- ✅ 使用深灰邊框代替藍色

---

### 4️⃣ 主要動作按鈕 (#actionBtn)

#### 狀態 1: Idle（閒置 - 啟動）

**Before**:
```css
background: qlineargradient(#6366f1 → #8b5cf6);  /* 藍紫漸層 */
box-shadow: 0 0 20px rgba(99, 102, 241, 0.6);  /* 發光 */
```

**After**:
```css
background: #335eea;  /* Anytype Blue 純色 */
/* 無發光效果 */
```

**Hover**:
- Before: 更亮的漸層 + 發光
- After: #2651dd (純色變暗)

---

#### 狀態 2: Running（運行中 - 停止）

**Before**:
```css
background: qlineargradient(#ef4444 → #b91c1c);  /* 紅色漸層 */
box-shadow: 0 0 20px rgba(239, 68, 68, 0.6);  /* 發光 */
```

**After**:
```css
background: #e03131;  /* Alert Red 純色 */
/* 無發光效果 */
```

---

#### 狀態 3: Starting/Stopping（處理中）

**Before**:
```css
background: #334155;  /* 藍灰 */
color: #64748b;
```

**After**:
```css
background: #262626;  /* 深灰卡片 */
color: #555555;  /* 暗灰文字 */
```

---

### 5️⃣ 快速連結按鈕

#### 主要按鈕 (#primaryLinkBtn)

**Before**:
```css
background: rgba(99, 102, 241, 0.15);  /* 藍色半透明 */
color: #818cf8;
border: 2px solid rgba(99, 102, 241, 0.5);
```

**After**:
```css
background: #262626;  /* Card Style 實色 */
color: #e5e5e5;  /* 灰白文字 */
border: 1px solid #333333;  /* 極細邊框 */
```

**Hover**:
- Before: 更亮的半透明
- After: #333333 (實色變亮)

---

#### 次要按鈕 (#linkBtn)

**Before**:
```css
background: rgba(129, 140, 248, 0.1);
color: #a5b4fc;  /* 藍紫色 */
border: 2px solid rgba(129, 140, 248, 0.3);
```

**After**:
```css
background: #1e1e1e;  /* 更深的卡片 */
color: #aaaaaa;  /* 中性灰 */
border: 1px solid #2a2a2a;
```

---

### 6️⃣ 控制台 (Console)

**Before**:
```css
background: #020617;  /* 深藍黑 */
color: #22d3ee;  /* 青色 */
```

**After**:
```css
background: #0f0f0f;  /* 接近純黑 */
color: #4ade80;  /* 終端機綠 */
```

**設計理念**:
- 模擬真實終端機的黑綠配色
- 提供更專業的技術感

---

### 7️⃣ 下拉選單 (QComboBox)

**Before**:
```css
background: rgba(99, 102, 241, 0.1);
border: 2px solid rgba(99, 102, 241, 0.3);
down-arrow: #818cf8;  /* 藍紫色 */
```

**After**:
```css
background: #262626;
border: 1px solid #333333;
down-arrow: #888888;  /* 中性灰 */
```

**下拉菜單項目**:
- Before: #1e293b (深藍) + 藍紫選取
- After: #1e1e1e (中性黑) + Anytype Blue 選取

---

### 8️⃣ 清空按鈕 (#clearBtn)

**Before**:
```css
background: rgba(255, 255, 255, 0.1);
color: #94a3b8;  /* 灰藍 */
```

**After**:
```css
background: #1e1e1e;
color: #888888;  /* 中性灰 */
```

---

## 📊 視覺對比總結

### 色彩溫度
- **Before**: 冷色調（藍/紫）
- **After**: 中性/暖灰

### 視覺重量
- **Before**: 輕盈、半透明、發光
- **After**: 扎實、實色、啞光

### 設計風格
- **Before**: 賽博龐克、霓虹、未來感
- **After**: 極簡主義、專業、沉穩

### 互動反饋
- **Before**: 發光 + 漸層變化
- **After**: 純色明暗變化

---

## 🎯 設計原則

### Anytype Design Language

1. **Void Black (深空黑)**
   - 背景使用接近純黑的深灰
   - 營造沉浸式體驗

2. **Matte Surface (啞光表面)**
   - 移除所有半透明和發光效果
   - 使用實色背景增加扎實感

3. **Minimal Borders (極細邊框)**
   - 邊框寬度減至 1px
   - 顏色選用深灰而非彩色

4. **No Gradients (去漸層化)**
   - 所有元素使用純色
   - Hover/Pressed 通過明暗變化反饋

5. **Subtle Interactions (微妙互動)**
   - 移除誇張的發光和陰影
   - 使用輕微的顏色變化

6. **Monochrome + Accent (單色 + 強調色)**
   - 主要使用黑白灰
   - 僅在主要動作使用 Anytype Blue
   - 警告動作使用 Alert Red

---

## 🔧 技術實現

### 修改範圍

**✅ 已修改**:
- `apply_styles()` 方法的完整 QSS
- 視窗標題（添加 "Anytype Edition"）
- 版本號標籤
- Logo 顏色（#6366f1 → #e5e5e5）
- 所有標籤文字顏色（#8be9fd 等 → #888888）

**✅ 保持不變**:
- 所有 Python 邏輯
- LauncherWorker 執行緒
- 狀態管理機制
- 多語言支援
- Docker 檢查
- 端口檢測
- 進程管理

---

## 📝 使用說明

### 啟動新主題

```powershell
cd "c:\Users\bruce\PycharmProjects\企業級伺服器(Dify+RAGflow)\BruV_Project"
python launcher_gui.py
```

### 視覺檢查清單

```
□ 主視窗背景是否為純黑灰 (#191919)
□ 側邊欄是否為極夜黑 (#111111)
□ 邊框是否為極細 (1px #333333)
□ 啟動按鈕是否為 Anytype Blue (#335eea)
□ 停止按鈕是否為 Alert Red (#e03131)
□ 控制台文字是否為終端綠 (#4ade80)
□ 所有漸層和發光是否已移除
□ Hover 效果是否為純色變化
```

---

## 🎨 配色速查表

```
主背景:    #191919  ████
側邊欄:    #111111  ████
控制台:    #0f0f0f  ████
卡片:      #262626  ████
邊框:      #333333  ████

主文字:    #e5e5e5  ████
次文字:    #888888  ████
暗文字:    #555555  ████

Anytype Blue:  #335eea  ████
Alert Red:     #e03131  ████
Terminal Green:#4ade80  ████
```

---

**Anytype Theme** - 極簡、專業、扎實 🖤
