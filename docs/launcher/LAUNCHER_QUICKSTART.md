# 🎯 BruV Enterprise Desktop Launcher - 快速入門指南

## 🚀 三步驟啟動

### 方法 1：使用批次腳本（推薦）

```batch
# 雙擊執行
start_gui_launcher.bat
```

### 方法 2：手動啟動

```powershell
# 1. 啟動虛擬環境
& "C:/Users/bruce/PycharmProjects/企業級伺服器(Dify+RAGflow)/.venv/Scripts/Activate.ps1"

# 2. 切換目錄
cd "c:\Users\bruce\PycharmProjects\企業級伺服器(Dify+RAGflow)\BruV_Project"

# 3. 執行啟動器
python launcher_gui.py
```

## 📸 介面預覽

```
┌─────────────────────────────────────────────────────────────────┐
│  🚀 BruV Enterprise Launcher v3.0           [─] [✕]            │
├──────────────┬──────────────────────────────────────────────────┤
│              │  📋 SYSTEM CONSOLE               [🗑️ Clear]     │
│   BruV       │                                                  │
│ ENTERPRISE   │  🎯 BruV Enterprise 啟動器                       │
│              │  ========================================        │
│ 🌐 語言      │  🐳 檢查 Docker 服務...                          │
│ [中文 ▼]     │  ✅ Docker 服務運行中                            │
│              │  🚀 啟動 FastAPI 後端服務...                     │
│ ┌──────────┐ │  ✅ 後端服務已啟動 (PID: 12345)                  │
│ │🚀 啟動系統│ │  ⏳ 等待服務在 localhost:8000 啟動...            │
│ └──────────┘ │  ✅ 服務已就緒 (localhost:8000) - 耗時 5.2s     │
│              │  🎨 啟動 Vue 前端服務...                         │
│ 快速連結      │  ✅ 前端服務已啟動 (PID: 12346)                  │
│ ┌──────────┐ │  ⏳ 等待服務在 localhost:5173 啟動...            │
│ │🚀開啟BruV │ │  ✅ 服務已就緒 (localhost:5173) - 耗時 8.1s     │
│ └──────────┘ │  ========================================        │
│ ┌──────────┐ │  🎉 系統啟動完成！                               │
│ │🌐開啟Dify │ │  ========================================        │
│ └──────────┘ │  🔌 Backend:  http://localhost:8000            │
│ ┌──────────┐ │  📖 API Docs: http://localhost:8000/docs       │
│ │🧠開RAGFlow│ │  🎨 Frontend: http://localhost:5173            │
│ └──────────┘ │  🌐 Dify:     http://localhost:82              │
│              │  🧠 RAGFlow:  http://localhost:81              │
│ ⚙️ 系統狀態   │  ========================================        │
│ ● Backend    │                                                  │
│ ● Frontend   │                                                  │
│ ● Docker     │                                                  │
│              │                                                  │
│ v3.0.0 企業版 │                                                  │
└──────────────┴──────────────────────────────────────────────────┘
```

## 🎨 按鈕狀態說明

### 啟動按鈕（中央大按鈕）

| 狀態 | 顯示文字 | 顏色 | 可點擊 | 說明 |
|------|---------|------|--------|------|
| 閒置 | 🚀 啟動系統 | 藍紫漸層 | ✅ | 點擊開始啟動 |
| 啟動中 | ⚙️ 啟動中... | 灰色 | ❌ | 正在啟動服務 |
| 運行中 | 🛑 停止系統 | 紅色漸層 | ✅ | 點擊停止系統 |
| 停止中 | ⏳ 停止中... | 灰色 | ❌ | 正在停止服務 |
| 失敗 | ❌ 啟動失敗 | 灰色 | ❌ | 3秒後自動恢復 |

### 狀態指示燈

| 顏色 | 狀態 | 說明 |
|------|------|------|
| 🟢 綠色 | Running | 服務正常運行 |
| ⚪ 灰色 | Stopped | 服務已停止 |
| 🔴 紅色 | Error | 服務錯誤 |
| 🟠 橙色 | Starting | 正在啟動 |

## 📱 使用案例

### 案例 1：日常啟動工作環境

```
1. 雙擊 start_gui_launcher.bat
2. 等待 GUI 視窗出現
3. 點擊 [🚀 啟動系統]
4. 觀察控制台日誌（約 10-30 秒）
5. 看到 "🎉 系統啟動完成！"
6. 點擊 [🚀 開啟 BruV AI]
7. 開始工作
```

### 案例 2：切換到英文介面

```
1. 點擊語言下拉選單
2. 選擇 "English"
3. 介面立即更新為英文
   - 🚀 START SYSTEM
   - QUICK LINKS
   - SYSTEM STATUS
   - SYSTEM CONSOLE
```

### 案例 3：檢查服務狀態

```
觀察左側狀態指示燈：

✅ 全綠 → 所有服務正常
● Backend   🟢  ← FastAPI 運行中
● Frontend  🟢  ← Vue 運行中
● Docker    🟢  ← Dify/RAGFlow 運行中

⚠️ 有灰色 → 某些服務未啟動
● Backend   🟢
● Frontend  ⚪  ← 前端未啟動
● Docker    🟢

❌ 有紅色 → 服務錯誤
● Backend   🔴  ← 後端啟動失敗
● Frontend  ⚪
● Docker    🟢
```

### 案例 4：優雅停止系統

```
1. 點擊 [🛑 停止系統]
2. 按鈕變灰顯示 "⏳ 停止中..."
3. 控制台顯示：
   =========================================
   🛑 正在停止系統...
   =========================================
   ✅ 所有服務已停止
   =========================================
4. 狀態燈全部變灰
5. 按鈕恢復為 [🚀 啟動系統]
```

### 案例 5：快速開啟特定服務

```
不想啟動完整系統？

1. 確保 Docker 已運行
2. 點擊 [🌐 開啟 Dify] 
   → 瀏覽器開啟 http://localhost:82
3. 點擊 [🧠 開啟 RAGFlow]
   → 瀏覽器開啟 http://localhost:81

注意：需要先手動啟動 Docker Compose
```

## 🔧 故障排除

### 問題 1：按鈕顯示 "❌ 啟動失敗"

**可能原因**：
- Docker 未運行
- 端口被佔用
- 虛擬環境異常

**解決方案**：
```powershell
# 1. 檢查 Docker
docker ps

# 2. 檢查端口
netstat -ano | findstr "8000"
netstat -ano | findstr "5173"

# 3. 檢查虛擬環境
python --version  # 應顯示 Python 3.11+
```

### 問題 2：控制台顯示 "❌ 服務啟動超時"

**可能原因**：
- 電腦效能不足
- 首次啟動需要更多時間
- 依賴套件未安裝完整

**解決方案**：
```powershell
# 1. 手動測試後端
python app_anytype.py

# 2. 手動測試前端
cd frontend
npm run dev

# 3. 重新安裝依賴
pip install -r requirements.txt
cd frontend
npm install
```

### 問題 3：視窗無法拖曳

**可能原因**：
- 點擊位置不在標題列
- 滑鼠事件被攔截

**解決方案**：
- 點擊標題列（最上方藍色區域）
- 避免點擊按鈕區域

### 問題 4：語言切換無效

**可能原因**：
- 翻譯鍵值遺失
- 程式碼錯誤

**解決方案**：
```powershell
# 重新啟動 GUI
python launcher_gui.py
```

### 問題 5：無法安裝 PySide6

**錯誤訊息**：
```
ERROR: Could not find a version that satisfies the requirement PySide6
```

**解決方案**：
```powershell
# 1. 升級 pip
python -m pip install --upgrade pip

# 2. 使用清華鏡像源
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple PySide6==6.6.1

# 3. 或使用官方源
pip install --upgrade PySide6
```

## 📊 效能優化建議

### 1. 加速啟動時間
```powershell
# 預先啟動 Docker
docker compose up -d

# 使用 SSD 安裝專案
# 避免將專案放在機械硬碟
```

### 2. 減少資源佔用
```python
# 修改 launcher_gui.py 中的超時設定
def wait_for_port(self, port, timeout=30):  # 從 60 改為 30
    ...
```

### 3. 背景執行
```powershell
# 將 GUI 最小化到托盤（未來功能）
# 當前可以點擊 [─] 最小化到工作列
```

## 🎓 進階技巧

### 1. 自訂快捷鍵
```python
# 在 launcher_gui.py 中添加
def keyPressEvent(self, event):
    if event.key() == Qt.Key_F5:
        self.toggle_system()  # F5 切換啟動/停止
    elif event.key() == Qt.Key_F1:
        self.open_url("http://localhost:5173")  # F1 開啟 BruV
```

### 2. 修改主題顏色
```python
# 搜尋 apply_styles() 方法
# 修改漸層顏色
background: qlineargradient(
    x1:0, y1:0, x2:1, y2:1,
    stop:0 #YOUR_COLOR_1,
    stop:1 #YOUR_COLOR_2
);
```

### 3. 添加新的快速連結
```python
# 在 create_sidebar() 中添加
new_btn = QPushButton("🔗 新服務")
new_btn.setObjectName("linkBtn")
new_btn.clicked.connect(lambda: self.open_url("http://localhost:XXXX"))
layout.addWidget(new_btn)
```

## 📚 相關文件

- [完整功能說明](LAUNCHER_FEATURES.md)
- [GUI 安裝指南](README_GUI_LAUNCHER.md)
- [專案整體說明](README.md)
- [API 整合文件](API_INTEGRATION.md)

## 💡 小技巧

### Windows 快速啟動
```batch
REM 創建桌面捷徑
REM 右鍵 → 內容 → 目標：
"C:\Users\bruce\PycharmProjects\企業級伺服器(Dify+RAGflow)\BruV_Project\start_gui_launcher.bat"

REM 圖示：選擇一個好看的 .ico 檔案
```

### PowerShell 別名
```powershell
# 在 PowerShell Profile 中添加
function Start-BruVLauncher {
    cd "c:\Users\bruce\PycharmProjects\企業級伺服器(Dify+RAGflow)\BruV_Project"
    python launcher_gui.py
}
Set-Alias bruv Start-BruVLauncher

# 使用方式
PS> bruv  # 一鍵啟動
```

### 開機自動啟動
```batch
REM 將 start_gui_launcher.bat 放到啟動資料夾
Win + R → shell:startup → 貼上捷徑
```

---

**享受專業級的桌面啟動體驗！** 🎉
